doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
    h1 Missing Dog Information
    h3 selected dog:  #{resultName}

    form(action=`/dogs/${thisDog}/editdog` method="post")
        button Dog Has been found, remove from list
    br
    hr
    h3 Please update dog info so we can help find them! 
    form(action=`/dogs/${thisDog}/updatedog` method="POST")
        label(name="name") Dog's name:
        input(type="text" name="name")
        br
        label(name="location_lost") Location Lost:
        input(type="text" name="location_lost")
        br
        label(name="description") Description: 
        input(type="text" name="description")
        br
        label(name="content") Notes and other information: 
        input(type="text" name="content")
        br
        label(name="owner_name") Owner Name: 
        input(type="text" name="owner_name")
        br
        label(name="owner_phone") Owner Phone: 
        input(type="text" name="owner_phone")
        br
        label(name="owner_email") Owner Email: 
        input(type="text" name="owner_email")
        br
        button Update
        //- button Update Their information
    p Thank You, I'm sure this helps.
    a(href='/dogs')
        button Back
